Ada_main2.m implements Adaboost algorithm which type of Boosting model and broadly categorized under Ensemble learning. It uses 500 weak decision trees and and an ensemble or collection of that makes the final decision.

The model is tested on randomly generated data from normal distribution.

decision_stump2.m is the function called by main function to create a decision tree.  